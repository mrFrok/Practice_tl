# Python-зависимости проекта.
# Фаза 1 (SSR): нужен только Django и драйвер PostgreSQL.
# DRF, cors-headers и пр. добавим в Фазе 2, когда дойдём до API.

Django>=5.2,<5.3          # веб-фреймворк (5.2 — LTS, долгая поддержка)
psycopg[binary]>=3.2      # драйвер PostgreSQL для Python (v3, с готовыми бинарниками)

# Тесты
pytest>=8.0               # тестовый фреймворк
pytest-django>=4.9        # интеграция pytest c Django (тестовая БД, настройки)
